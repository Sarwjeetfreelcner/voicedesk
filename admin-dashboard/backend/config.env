# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=voiceaiuser
DB_PASSWORD=P@ssw0rd12345
DB_NAME=voice-ai

# Server Configuration
PORT=5000
NODE_ENV=production

# JWT Secret (Change this in production!)
JWT_SECRET=voicedesk-ai-agent-platform-super-secret-jwt-token-2025-production

# Email Configuration (SMTP Relay - SodaHost)
# IP-based authentication - no username/password needed
SMTP_HOST=relay.sodahost.co.uk
SMTP_PORT=26
SMTP_SECURE=false
SMTP_FROM_EMAIL=noreply@voicedesk.ai
SMTP_FROM_NAME=VoiceDesk AI Admin

# Email Verification (disabled for now since signup UI not ready)
REQUIRE_EMAIL_VERIFICATION=false
VERIFICATION_TOKEN_EXPIRY=24h

# Default Admin Credentials (for initial setup)
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=admin123
DEFAULT_ADMIN_EMAIL=admin@voicedesk.ai

# Application URL (for email links) - Using server IP
# Replace with your domain when you get one
APP_URL=http://195.34.79.69:3000

